#include<stdio.h>
int main(){
    int x,i,ra=0;
    scanf("%d",&x);
    for(i=1;i<=x;i++){
        if(x%i==0)
                ra++;
        if(ra==1)
             printf("%d\n",i);
             ra=0;
        }
	return 0;
}
